#include<stdio.h>
int main() {
    int m;
    printf("enter the value m:");
    scanf("%d", &m);

    if(m != 0)
     if(m > 0) {
        printf("the value of n =1");
    } else {
        printf("the value of n=-1");
      } 
      else {
        printf("the value of n= 0");
      }
}